export { generateSiteMetadata, buildMetadataFromSettings, getSeoSettings } from "@/lib/seo/metadata";
export {
  buildHomeSchemaGraph,
  buildMenuPageSchemaGraph,
  buildRestaurantSchema,
  buildLocalBusinessSchema,
  buildMenuSchema,
} from "@/lib/seo/schema";
export { absoluteUrl, getSiteUrl, resolveImageUrl } from "@/lib/seo/url";
